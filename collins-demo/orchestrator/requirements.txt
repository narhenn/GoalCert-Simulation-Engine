# Collins agentic demo — orchestrator (BFF) dependencies.
# Installed into the repo-root .venv (see demo.ps1), reusing the existing env.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
anthropic>=0.40
pydantic>=2.0
python-dotenv>=1.0

# image->3D production pipeline (pipeline3d/) + BIM ingest (bim_ifc.py)
pillow>=10.0
numpy>=1.26
trimesh>=4.0
ifcopenshell>=0.8
python-multipart>=0.0.9
# optional: local background matting for the 2D->3D preprocess layer
# (the GPU worker does it server-side regardless)
# rembg>=2.0
